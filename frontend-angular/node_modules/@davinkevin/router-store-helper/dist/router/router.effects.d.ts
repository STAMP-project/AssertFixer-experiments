import { Actions } from '@ngrx/effects';
import { RouterNavigateAction, RouterNavigateByUrlAction } from './router.actions';
import { Router } from '@angular/router';
import { Observable } from 'rxjs/Observable';
export declare class RouterEffects {
    private actions$;
    private router;
    navigate$: Observable<RouterNavigateAction>;
    navigateByUrl$: Observable<RouterNavigateByUrlAction>;
    constructor(actions$: Actions, router: Router);
}
