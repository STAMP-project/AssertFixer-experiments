import { StompConfiguration } from './configuration';
import { Client } from 'stompjs';
import { Observable } from 'rxjs/Observable';
export interface Stomp {
    client: (url: string, protocols?: string | Array<string>) => Client;
    over: (ws: WebSocket) => Client;
}
export declare class StompService {
    private conf;
    private stomp;
    private readonly cl;
    private readonly connection$;
    constructor(conf: StompConfiguration, stomp: Stomp, document: any);
    on<T>(topic: string): Observable<T>;
}
