package hu.elte.recipe.entities;

// TODO: Auto-generated Javadoc
/**
 * The Enum IngredientUnitType.
 */
public enum IngredientUnitType {
  
  /** The kg. */
  KG, /** The dkg. */
 DKG, /** The ek. */
 EK, /** The db. */
 DB, /** The l. */
 L, /** The kk. */
 KK, /** The dl. */
 DL, /** The ml. */
 ML, /** The tk. */
 TK, /** The egy keves. */
 EGY_KEVES, /** The csipet. */
 CSIPET
}
