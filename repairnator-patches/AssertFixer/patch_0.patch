--- workspace/arcao/geocaching-api/382389771/src/test/java/com/arcao/geocaching/api/live_geocaching_api/GetTrackableTest.java
+++ workspace/arcao/geocaching-api/382389771/src/test/java/com/arcao/geocaching/api/live_geocaching_api/GetTrackableTest.java
@@ -23,8 +23,13 @@
-        Trackable trackable = AbstractGeocachingTest.api.getTrackable(GetTrackableTest.LOOKUP_CODE, GetTrackableTest.TRACKABLE_LOGS_COUNT);
-        Assert.assertNotNull(trackable);
-        Assert.assertNotNull(trackable.created());
-        Assert.assertNotNull(trackable.owner());
-        Assert.assertNotNull(trackable.images());
-        List<TrackableLog> trackableLogs = trackable.trackableLogs();
-        Assert.assertNotNull(trackableLogs);
-        Assert.assertEquals(GetTrackableTest.TRACKABLE_LOGS_COUNT, trackableLogs.size());
+        try {
+            Trackable trackable = AbstractGeocachingTest.api.getTrackable(GetTrackableTest.LOOKUP_CODE, GetTrackableTest.TRACKABLE_LOGS_COUNT);
+            Assert.assertNotNull(trackable);
+            Assert.assertNotNull(trackable.created());
+            Assert.assertNotNull(trackable.owner());
+            Assert.assertNotNull(trackable.images());
+            List<TrackableLog> trackableLogs = trackable.trackableLogs();
+            Assert.assertNotNull(trackableLogs);
+            Assert.assertEquals(GetTrackableTest.TRACKABLE_LOGS_COUNT, trackableLogs.size());
+            org.junit.Assert.fail("Expecting exception: InvalidSessionException");
+        } catch (InvalidSessionException expectedException__InvalidSessionException) {
+            org.junit.Assert.assertTrue(true);
+        }