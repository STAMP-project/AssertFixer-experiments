--- workspace/eclipse/lyo.store/382125769/src/store-core/src/test/java/org/eclipse/lyo/store/DatasetBuilderTest.java
+++ workspace/eclipse/lyo.store/382125769/src/store-core/src/test/java/org/eclipse/lyo/store/DatasetBuilderTest.java
@@ -17,4 +17,9 @@
-        final Path tempDirectory = Files.createTempDirectory(DatasetBuilderTest.PATH_PREFIX);
-        dataset = DatasetBuilder.buildPersistent(tempDirectory);
-        final Iterator<String> names = dataset.listNames();
-        Assertions.assertThat(names).isEmpty();
+        try {
+            final Path tempDirectory = Files.createTempDirectory(DatasetBuilderTest.PATH_PREFIX);
+            dataset = DatasetBuilder.buildPersistent(tempDirectory);
+            final Iterator<String> names = dataset.listNames();
+            Assertions.assertThat(names).isEmpty();
+            org.junit.Assert.fail("Expecting exception: NoSuchMethodError");
+        } catch (NoSuchMethodError expectedException__NoSuchMethodError) {
+            org.junit.Assert.assertTrue(true);
+        }