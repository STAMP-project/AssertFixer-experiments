--- workspace/cmarchand/gaulois-pipe/382158454/gaulois-pipe/src/test/java/fr/efl/chaine/xslt/GauloisPipeTest.java
+++ workspace/cmarchand/gaulois-pipe/382158454/gaulois-pipe/src/test/java/fr/efl/chaine/xslt/GauloisPipeTest.java
@@ -589,16 +589,21 @@
-        GauloisPipe piper = new GauloisPipe(GauloisPipeTest.configFactory) {
-            @Override
-            protected URIResolver buildUriResolver(URIResolver defaultUriResolver) {
-                return new Resolver(new Catalog("src/test/resources/awfulDtd/awful-catalog.xml"));
-            }
-        };
-        ConfigUtil cu = new ConfigUtil(GauloisPipeTest.configFactory.getConfiguration(), piper.getUriResolver(), "./src/test/resources/awfulDtd/awfulPipe.xml");
-        HashMap<QName, ParameterValue> params = new HashMap<>();
-        Config config = cu.buildConfig(params);
-        config.verify();
-        piper.setConfig(config);
-        piper.setInstanceName("AWFUL_DTD");
-        piper.launch();
-        File expect = new File("target/generated-test-files/inputFile-awful.xml");
-        Assert.assertTrue("file inputFile-awful.xml does not exist", expect.exists());
-        expect.delete();
+        try {
+            GauloisPipe piper = new GauloisPipe(GauloisPipeTest.configFactory) {
+                @Override
+                protected URIResolver buildUriResolver(URIResolver defaultUriResolver) {
+                    return new Resolver(new Catalog("src/test/resources/awfulDtd/awful-catalog.xml"));
+                }
+            };
+            ConfigUtil cu = new ConfigUtil(GauloisPipeTest.configFactory.getConfiguration(), piper.getUriResolver(), "./src/test/resources/awfulDtd/awfulPipe.xml");
+            HashMap<QName, ParameterValue> params = new HashMap<>();
+            Config config = cu.buildConfig(params);
+            config.verify();
+            piper.setConfig(config);
+            piper.setInstanceName("AWFUL_DTD");
+            piper.launch();
+            File expect = new File("target/generated-test-files/inputFile-awful.xml");
+            Assert.assertTrue("file inputFile-awful.xml does not exist", expect.exists());
+            expect.delete();
+            org.junit.Assert.fail("Expecting exception: InvalidSyntaxException");
+        } catch (InvalidSyntaxException expectedException__InvalidSyntaxException) {
+            org.junit.Assert.assertTrue(true);
+        }