--- workspace/codeu-su2018-team3/CodeU-Summer-2018/383007382/src/test/java/codeu/controller/ChatServletTest.java
+++ workspace/codeu-su2018-team3/CodeU-Summer-2018/383007382/src/test/java/codeu/controller/ChatServletTest.java
@@ -130,5 +130,10 @@
-        Mockito.when(mockSession.getAttribute("user")).thenReturn("test_username");
-        Mockito.when(mockUserStore.getUser("test_username")).thenReturn(null);
-        chatServlet.doPost(mockRequest, mockResponse);
-        Mockito.verify(mockMessageStore, Mockito.never()).addMessage(Mockito.any(Message.class));
-        Mockito.verify(mockResponse).sendRedirect("/login");
+        try {
+            Mockito.when(mockSession.getAttribute("user")).thenReturn("test_username");
+            Mockito.when(mockUserStore.getUser("test_username")).thenReturn(null);
+            chatServlet.doPost(mockRequest, mockResponse);
+            Mockito.verify(mockMessageStore, Mockito.never()).addMessage(Mockito.any(Message.class));
+            Mockito.verify(mockResponse).sendRedirect("/login");
+            org.junit.Assert.fail("Expecting exception: ArgumentsAreDifferent");
+        } catch (ArgumentsAreDifferent expectedException__ArgumentsAreDifferent) {
+            org.junit.Assert.assertTrue(true);
+        }