--- workspace/sflin/ASER_Parser/380628283/src/test/java/parser/ParserTest.java
+++ workspace/sflin/ASER_Parser/380628283/src/test/java/parser/ParserTest.java
@@ -14,7 +14,12 @@
-        String[] args = new String[]{ contextArchive, path };
-        Parser.main(args);
-        File archive = new File(path);
-        Assert.assertTrue(archive.exists());
-        Assert.assertTrue(archive.isDirectory());
-        File[] files = archive.listFiles();
-        Assert.assertEquals(files.length, 3);
+        try {
+            String[] args = new String[]{ contextArchive, path };
+            Parser.main(args);
+            File archive = new File(path);
+            Assert.assertTrue(archive.exists());
+            Assert.assertTrue(archive.isDirectory());
+            File[] files = archive.listFiles();
+            Assert.assertEquals(files.length, 3);
+            org.junit.Assert.fail("Expecting exception: IllegalArgumentException");
+        } catch (IllegalArgumentException expectedException__IllegalArgumentException) {
+            org.junit.Assert.assertTrue(true);
+        }