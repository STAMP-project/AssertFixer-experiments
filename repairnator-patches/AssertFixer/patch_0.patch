--- workspace/runelite/runelite/379703740/deobfuscator/src/test/java/net/runelite/deob/updater/UpdateMappingsTest.java
+++ workspace/runelite/runelite/379703740/deobfuscator/src/test/java/net/runelite/deob/updater/UpdateMappingsTest.java
@@ -23,8 +23,13 @@
-        File client = new File(properties.getRsClient());
-        ClassGroup group1 = JarUtil.loadJar(client);
-        ClassGroup group2 = JarUtil.loadJar(client);
-        // Remove existing annotations
-        unannotate(group2);
-        // Map the client against itself
-        map(group1, group2);
-        check(group1, group2);
+        try {
+            File client = new File(properties.getRsClient());
+            ClassGroup group1 = JarUtil.loadJar(client);
+            ClassGroup group2 = JarUtil.loadJar(client);
+            // Remove existing annotations
+            unannotate(group2);
+            // Map the client against itself
+            map(group1, group2);
+            check(group1, group2);
+            org.junit.Assert.fail("Expecting exception: FileNotFoundException");
+        } catch (FileNotFoundException expectedException__FileNotFoundException) {
+            org.junit.Assert.assertTrue(true);
+        }