-- Find existing nodes
-- This is a stored function, see the structure definition, first decleration V4.1.1_1
-- sig: find_existing_nodes(nodeUidList, graphList)
SELECT * FROM find_existing_nodes(?, ?);
