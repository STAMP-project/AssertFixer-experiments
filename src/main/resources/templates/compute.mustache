<title>My application deployed automatically</title>
<font size="-4"><h1>This method builds a random string from:</h1>
<h1>i)Choosing even numbers from list 1(box 1, hit enter every time when a new integer is written.)</h1>
<h1>ii)Choosing odd numbers from list 2(box 2, hit enter every time when a new integer is written.)</h1>
<h1>iii)Getting characters with chosen numbers from randomly chosen string from list 3(box 3, hit enter every time when a new String is written.)</h1>
<h1>iv)Returns the builtstring as (4th boxes value - enter just 1 integer!) times</h1></font>

  Result is {{result}}




  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input2" rows="10" cols="30"></textarea>
        <textarea name="input3" rows="10" cols="30"></textarea>
        <textarea name="input4" rows="10" cols="30"></textarea>
        <br>
        <input type="submit">
  </form>
