version: '2'
services:
    tradebravely-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb tradebravely-mongodb-config
    tradebravely-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    tradebravely-mongodb-config:
        image: mongo:3.6.3
        container_name: tradebravely-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
